# SciClaw v4.0.0-ga Release Notes

**Version**: v4.0.0-ga — the package version stays **4.0.0**
**Date**: 2026-09-21
**Theme**: GA — the production-ready determination is met, and verified
**Basis**: `inbox/inbox-results/ga-readiness-declaration-20260921.md` (six-criteria
verification). Nothing in this document claims more than that verification.

---

## What this milestone is

`v4.0.0-ga` is a **determination, not a new version**: the package version stays
4.0.0, no API changed, and nothing was published to a registry. What this tag
records is that the six criteria below were verified on this tree — and that the
repository becomes public with this milestone.

## Six-criteria readiness verification

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | Acceptance suite green, anti-cheat included | `npm run test:ga` → 26/26, CI-blocking; calibration R1 closed with an independent re-verification (PASS @ `d37d335`); eight anti-cheat regression locks are committed in-tree, so they cannot be lost by a clean checkout |
| 2 | Clean-machine three steps | fresh clone (main + 3 sibling repos) → install → `build` exit 0 → `research "CRISPR base editing efficiency"` on live sources: **15 real sources / 29 claims / 29 verdicts / four gates PASS** |
| 3 | CI green and visible | run @ `7745222` green (22.x matrix + lint job). The structural red that had persisted since v3.7.0 is cleared: family sibling dependencies now materialise inside CI. The matrix is pinned to 22.x because a family dependency requires Node >= 22.22.3 |
| 4 | Guard chain in place, and exercised | tag↔version equality (package + plugin manifest) and "Release body == in-repo notes", both fail-closed; `v4.0.0` passed it and this tag exercises it again |
| 5 | Public claims match measurement | README badge 959 + 26 GA; version 4.0.0; LICENSE and SECURITY state the 4.0.x support line; plugin description on current positioning; the word "verifiable" carries the calibration note below |
| 6 | No open P0 | both decision books ratified; remaining items are explicitly deferred to v4.0.x / v4.1 / v5.0.0 |

## Proven capabilities (measured, not asserted)

- **Live pipeline**: multi-source search → claim extraction → per-claim verifier
  verdicts → four quality gates → report with an embedded evidence block. Gate
  failure withdraws the conclusion instead of shipping it (fail-closed).
- **Acceptance suite** (fixture mode, zero network) asserts source provenance,
  citation coverage, gate enforcement, verifier verdict presence and golden
  outputs; a live variant checks the same structural invariants against the
  network.
- **Benchmark** now consumes real orchestration output: 3/3 tasks, average
  overall **0.769** (`benchmark-results.json`).
- **Build smoke**: the `dist` entry point loads and `sciclaw --version` runs — a
  class of breakage that CI could not see before.
- **Test baseline at this tag**: 946 passed / 4 skipped (`npm test`), plus the
  26-case GA suite.

## Calibration limits (read this before quoting "verifiable")

- Two bias factors — **confirmation** and **temporal** — are not measurable from
  the inputs currently available and are therefore **excluded from gate
  verdicts**. They return when the corpus expansion lands in the v4.0.x line.
- The live acceptance variant checks structural invariants; it is not a
  benchmark of research quality.
- Verification backs the **evidence chain** — source provenance, citation
  coverage, gate enforcement, verifier verdicts. It does not certify the factual
  correctness of synthesized prose.

## Distribution

- The repository is **public** as of this milestone (`opensourceclaw/sciclaw`).
- **No npm publication** — SciClaw has no registry face; adopt by cloning the
  repository. (The `sciclaw` / `@sciclaw` npm namespace claim is tracked
  separately.)
- Reproduce locally: `npm ci && npm run build && npm run test:ga` (fixture, no
  network) and `npm run benchmark`.

## Not in GA (explicitly deferred)

- **v4.0.x**: ROADMAP rewrite, dead-face removals, bias-corpus expansion.
- **v4.1**: runtime-contract migration (`~/.deepclaw` → `~/.sciclaw`, env name,
  project id, broadcast tag) and observe/monitoring convergence.
- **v5.0.0**: AutoResearchFlow promotion.

---

*Published: 2026-09-21*

---

## v4.0.0 — identity integration milestone（历史版本）

# SciClaw v4.0.0 Release Notes

**Version**: v4.0.0
**Date**: 2026-09-21
**Theme**: DeepClaw -> SciClaw identity integration milestone
**Plan**: `docs/plans/sciclaw-v4.0.0-plan.md`

---

## Release Summary

SciClaw v4.0.0 is the identity-integration milestone. The repository formerly
known as DeepClaw ships as **SciClaw — verifiable deep research for AI4S**: an
independent member of the agent family, sharing the ecosystem infrastructure
(claw-obs / claw-cog / claw-mem / claw-ctx) without depending on DevClaw.

In the same milestone the shared core package is merged back in, the
observability adapter is realigned with claw-obs v2.5.0, and the build/test
baseline is fully green. There are no functional changes to the research
pipeline in this release — it is a rename, a consolidation, and a baseline
hardening.

## Breaking change (identity only)

- **Package / binary**: `sciclaw` (the CLI was `deepclaw`). Plugin identity in
  `openclaw.plugin.json` is synced (it had been stale at 3.0.0).
- **Brand strings**: 147 files / 264 occurrences — README, SKILL, CONTRIBUTING,
  living docs, source comments and console prefixes — plus the three
  brand-bearing class names: `SciClawObserver`, `SciClawCogIntegration`,
  `SciClawLearningEngine`.
- **Flow class names stay**: `DeepResearchFlow` / `AutoResearchFlow` are stage
  semantics, not branding, and are unchanged.

## Runtime contracts: zero migration

Deliberately untouched in v4.0.0, so upgrading users migrate nothing. The
rename of these runtime identifiers is a separate task in the v4.0.x line, with
a compatibility read of the old location:

- `~/.deepclaw/*` data directories
- `DEEPCLAW_MEMORY_WORKSPACE`
- the `deepclaw` project-id default in the memory adapter
- the `"deepclaw"` claw-cog broadcast tag / sessionId prefix

## What changed

- **deepclaw-core merged into `src/core/`** (61 TS files) — single consumer, so
  the cross-repository `file:` dependency is gone and the library ships in-tree.
  `CORE_VERSION` stays `1.0.0`.
- **MetricsCollector ported** off the removed claw-obs v2.5.0 API; the now
  orphaned claw-obs `file:` dependency was dropped (zero imports).
- **dist and coverage snapshots rebuilt** after the merge (brand strings zeroed,
  core face included, stale artifacts dropped).

## Quality

- 900/900 tests passing across 71 test files (`npm test`).
- `tsc --noEmit` exit 0; `npm run build` exit 0.
- The tracked `dist/` snapshot is in sync: a full rebuild produces no tracked
  diff.

## Distribution

- **Private repository; no npm registry face** — v4.0.0 is not published to any
  registry and no dist-tags apply. Adoption is by clone (see README), or as an
  OpenClaw plugin through the platform-side installation path.

---

*Published: 2026-09-21*
